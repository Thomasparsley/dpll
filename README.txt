Ke spuštění zdrojového kódu je potřeba alespoň dotnet 6 (https://dotnet.microsoft.com/en-us/download).

V zdrojovém souboru `Main.cs` je proměnná `files` pomocí které se načítají dimacs soubory.

Program se spouští pomocí příkazu: `dotnet run -c Release Main.cs`